export enum LivenessStatuses {
  up = "up",
  down = "down",
}

export enum LivenessHealthStatuses {
  healthy = "healthy",
  unhealthy = "unhealthy",
}

export enum LivenessServices {
  database = "database",
}
